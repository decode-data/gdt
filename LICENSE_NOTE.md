This repository (the GDTO specification) is licensed under Apache 2.0 — see `LICENSE`.

Note: the reference implementation, `madflow-sqlops` (github.com/decode-data/madflow-sqlops), is a separate repository under the MIT license, matching `sqlglot`. The two license choices are deliberate and different — see this repo's README for the reasoning.
